---
phase: "01-jobs-page"
plan: "04"
type: "execute"
wave: 3
depends_on: ["01-03"]
files_modified: []
autonomous: true
requirements: ["JOBS-09", "JOBS-10", "JOBS-11"]

must_haves:
  truths:
    - "Job detail page opens in new window"
    - "Full job description displayed"
    - "Google ads displayed in detail page"
    - "Apply button at bottom opens in new tab"
  artifacts:
    - path: "src/views/JobDetail.vue"
      provides: "Job detail page with description, ads, apply button"
      min_lines: 100
    - path: "src/composables/useJobDetail.ts"
      provides: "Composable to fetch single job"
  key_links:
    - from: "JobDetail.vue"
      to: "Jobs.vue"
      via: "routerLink /jobs/:id"
      pattern: "routerLink.*jobs.*id"
    - from: "JobDetail.vue"
      to: "apply_url"
      via: "target _blank"
      pattern: "target.*blank"
---

<objective>
Implement job detail page that displays full job description, requirements, Google ads integration, and apply button that opens in a new tab.
</objective>

<execution_context>
@AGENTS.md
@.planning/phases/01-jobs-page/01-02-SUMMARY.md
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
# From Worker API
GET /jobs/:id returns single Job

Job fields (from job_postings table):
- id (UUID), title, company, company_logo_url, location
- job_type, is_remote, experience_years
- salary_min, salary_max, salary_currency
- skills (TEXT[]), description
- job_url, job_url_direct
- created_at, updated_at
</context>

<tasks>

<task type="auto">
<name>task 1: Create useJobDetail composable</name>
<files>src/composables/useJobDetail.ts</files>
<action>
Create src/composables/useJobDetail.ts:

- fetchJob(id) - fetches single job from API or router params
- Uses same caching from useJobs (don't refetch if already loaded)
- Loading, error, job states

Also update types/jobs.ts to include optional job from router.
</action>
<verify>TypeScript compiles</verify>
  <done>Composable created</done>
</task>

<task type="auto">
<parameter name="name>task 2: Create JobDetail.vue page</name>
<files>src/views/JobDetail.vue</files>
<action>
Create src/views/JobDetail.vue:

1. Hero section with job title, company, metadata:
   - Location badge
   - Experience range badge
   - Salary (if available)
   - Posted date

2. Content sections:
   - Job Description (full)
   - Requirements/Qualifications
   - Company info

3. Google Ads placement:
   - Insert Google ad at top of content (above description)
   - Or ad in right sidebar on desktop
   - Use existing Ads component or inline ad-unit div
   - Ad unit: responsive

4. Apply button:
   - Fixed at bottom OR prominent button at end
   - Opens apply_url in NEW TAB (target="_blank")
   - Large, accessible, Zentor CTA styling

5. Back link:
   - Link back to /jobs/ listing
</action>
<verify>npm run build passes</verify>
  <done>Job detail page renders with all sections</done>
</task>

<task type="auto">
<name>job 3: Add SEO and routing</name>
<files>src/views/JobDetail.vue</files>
<action>
JobDetail SEO in onMounted:
- title: "{Job Title} at {Company} | Zentor Jobs"
- meta description: First 160 chars of description
- og:title, og:description, og:url, og:image (use zentor logo)
- Dynamic based on job data

Route in src/main.js:
- path: '/jobs/:id/'
- name: 'JobDetail'
- component: () => import('./views/JobDetail.vue')

Pass job ID via route params. If job not in cache, fetch from API.
</action>
<verify>Route navigates correctly, SEO tags update for each job</verify>
  <done>SEO dynamic per job, route functional</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → External Apply URL | User navigates to external site |
| Ad Script → Page | Third-party ad script runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-04-01 | Clickjacking | Apply button | mitigate | target="_blank" without opener hijacking |
| T-04-02 | Tampering | Ad content | accept | Google serves ads, trusted |
</threat_model>

<verification>
[ ] Job detail displays full description
[ ] Apply button opens in new tab
[ ] Google ads render in page
[ ] Back to jobs link works
[ ] Dynamic SEO updates per job
</verification>

<success_criteria>
Job detail page:
- Opens in new window from Jobs listing
- Full job description displayed
- Google ads integrated
- Apply button at bottom, opens in new tab
- Dynamic SEO per job
</success_criteria>

<output>
After completion, create `.planning/phases/01-jobs-page/01-04-SUMMARY.md`
</output>