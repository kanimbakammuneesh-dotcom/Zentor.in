---
phase: "01-jobs-page"
plan: "05"
type: "execute"
wave: 4
depends_on: ["01-03", "01-04"]
files_modified: []
autonomous: true
requirements: ["JOBS-01", "JOBS-02", "JOBS-03", "JOBS-04", "JOBS-05", "JOBS-06", "JOBS-07", "JOBS-08", "JOBS-09", "JOBS-10", "JOBS-11"]

must_haves:
  truths:
    - "Jobs route accessible from navigation"
    - "Job cards link to detail in new window"
    - "All routes use trailing slashes"
    - "Site builds without errors"
  artifacts:
    - path: "src/main.js"
      provides: "Router with Jobs and JobDetail routes"
    - path: "src/components/NavBar.vue"
      provides: "Navigation with Jobs link"
---

<objective>
Final integration - add Jobs routes to router, update NavBar, verify complete build. This connects all previous plans into working system.
</objective>

<execution_context>
@AGENTS.md
@.planning/phases/01-jobs-page/01-03-SUMMARY.md
@.planning/phases/01-jobs-page/01-04-SUMMARY.md
</execution_context>

<context>
# From previous plans
Routes to add:
- /jobs/ → Jobs.vue
- /jobs/:id/ → JobDetail.vue

Current routes in src/main.js:
- / -> Home
- /courses/ -> Courses
- /admissions/ -> Admissions
- /internship/ -> Internship
- /referrals/ -> Referrals
- /privacy/ -> PrivacyPolicy
- /terms/ -> Terms
- /contact/ -> Contact
- /* -> Error
</context>

<tasks>

<task type="auto">
<name>task 1: Add Jobs routes to router</name>
<files>src/main.js</files>
<action>
Update src/main.js router:

```javascript
{
  path: '/jobs/',
  name: 'Jobs',
  component: () => import('./views/Jobs.vue')
},
{
  path: '/jobs/:id/',
  name: 'JobDetail',
  component: () => import('./views/JobDetail.vue')
},
```

Place before the catch-all /* route.

Ensure trailing slashes on all existing routes (verify from AGENTS.md).
</action>
<parameter name="verify">npm run build passes</verify>
  <done>Routes added to router</done>
</task>

<task type="auto">
<parameter name="task 2: Update NavBar with Jobs link</name>
<files>src/components/NavBar.vue</files>
<action>
Read current NavBar.vue, add Jobs link:

- Add "Jobs" nav item after "Internship"
- Link to /jobs/ route
- Use same styling as other nav items
- Mobile menu includes Jobs option
</action>
  <verify>NavBar renders with Jobs link</verify>
  <done>Navigation includes Jobs</done>
</task>

<task type="auto">
<name>task 3: Verify build and final checks</name>
<files>src/views/Jobs.vue, src/views/JobDetail.vue</files>
<action>
Run final checks:

1. npm run build - ensure no errors
2. Verify all routes work (grep for route paths)
3. Check trailing slashes on all /jobs/ routes
4. Verify components import correctly

Any fixes needed to pass build.
</action>
  <verify>npm run build passes with no errors</verify>
  <done>Full build passes</done>
</task>

</tasks>

<verification>
[ ] Routes added to router
[ ] NavBar includes Jobs link
[ ] npm run build passes
[ ] All routes use trailing slashes
</verification>

<success_criteria>
Integration complete:
- Jobs accessible from navigation
- Build passes without errors
- All routes trailing-slashed
- Job detail opens in new window
</success_criteria>

<output>
After completion, create `.planning/phases/01-jobs-page/01-01-SUMMARY.md` through `01-05-SUMMARY.md`
</output>