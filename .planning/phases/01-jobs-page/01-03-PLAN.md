---
phase: "01-jobs-page"
plan: "03"
type: "execute"
wave: 3
depends_on: ["01-02"]
files_modified: []
autonomous: true
requirements: ["JOBS-04", "JOBS-05", "JOBS-06", "JOBS-07", "JOBS-08"]

must_haves:
  truths:
    - "Job cards display with title, company, location, experience range, posted date"
    - "Users can filter jobs by location"
    - "Users can filter by years of experience"
    - "Users can search by company name"
    - "Users can sort by posted date"
    - "Pagination navigates through job results"
    - "Jobs data cached locally after first fetch"
  artifacts:
    - path: "src/views/Jobs.vue"
      provides: "Jobs listing page with cards, filters, pagination"
      min_lines: 150
    - path: "src/composables/useJobs.ts"
      provides: "Composable for jobs data fetching and caching"
      min_lines: 60
    - path: "src/types/jobs.ts"
      provides: "Job and JobCard types"
  key_links:
    - from: "src/views/Jobs.vue"
      to: "/jobs-api"
      via: "useJobs composable"
      pattern: "fetch.*jobs.*api"
    - from: "useJobs.ts"
      to: "localStorage"
      via: "cached jobs data"
      pattern: "localStorage"
---

<objective>
Implement Vue.js jobs listing page with card-based job display, local storage caching, filtering by location/experience, company search, date sorting, and pagination. Follows existing Zentor design system from AGENTS.md.
</objective>

<execution_context>
@AGENTS.md
@.planning/phases/01-jobs-page/01-02-SUMMARY.md
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
# From Worker API (01-02)
GET /jobs returns:
{
  jobs: Job[],
  total: number,
  page: number,
  hasMore: boolean
}

Filter params: location, company, min_exp, max_exp, sort, page, limit

Job fields:
- id, title, company, location, description, requirements
- experience_min, experience_max, apply_url
- salary_min, salary_max
- created_at, last_updated
</context>

<tasks>

<task type="auto">
<name>task 1: Create Job types and useJobs composable</name>
<files>src/types/jobs.ts, src/composables/useJobs.ts</files>
<action>
Create src/types/jobs.ts:

```typescript
export interface Job {
  id: string;
  title: string;
  company: string;
  location: string;
  description: string;
  requirements: string;
  experience_min: number;
  experience_max: number;
  apply_url: string;
  salary_min?: string;
  salary_max?: string;
  created_at: string;
  last_updated: string;
}

export interface JobsResponse {
  jobs: Job[];
  total: number;
  page: number;
  hasMore: boolean;
}
```

Create src/composables/useJobs.ts:
- fetchJobs(filters) - fetches from API, caches in localStorage
- Caches jobs data with timestamp
- auto-refresh if cache > 1 hour old
- Manual refresh via trigger URL
- Loading and error states

Use reactive state for filters: location, company, minExp, maxExp, sort, page.
</action>
<verify>Types valid, composable imports without errors</verify>
<done>Types and composable with caching implemented</done>
</task>

<task type="auto">
<name>task 2: Create Jobs listing page with filters</name>
<files>src/views/Jobs.vue</files>
<action>
Create src/views/Jobs.vue with HeroSection + CardGrid pattern:

1. HeroSection with:
   - eyebrow: "Careers"
   - headline: "Find Your Dream Job"
   - tagline: "Apply for Tech Jobs"

2. Search/Filter bar:
   - Location dropdown (filter)
   - Company search input
   - Experience range dropdown (0-1, 1-3, 3-5, 5+)
   - Sort by date (newest first default)

3. Job cards grid:
   - Map jobs from useJobs composable
   - Card shows: title, company, location, experience, posted date
   - Click card → opens JobDetail in new tab (/jobs/:id)
   - Use existing CardGrid component or custom job-card styling

4. Pagination:
   - Page numbers + prev/next
   - Total count display

Mobile-first: Filters collapse into dropdown on mobile.

Follow Zentor color scheme: var(--glass-bg), var(--text), var(--acid), etc.
</action>
<verify>npm run build passes, page displays</verify>
<done>Jobs listing with all filters functional</done>
</task>

<task type="auto">
<name>task 3: Add SEO and navigation</name>
<files>src/views/Jobs.vue</files>
<action>
Add onMounted SEO:
- title: "Jobs | Zentor"
- meta description: "Find tech jobs in India. Filter by location, experience. Apply to top companies."
- og:title, og:description, og:url, og:image
- twitter:card

Add router route in src/main.js:
- path: '/jobs/'
- name: 'Jobs'
- component: () => import('./views/Jobs.vue')

Update NavBar with Jobs link.
</action>
<verify>Route works, SEO tags present</verify>
<done>SEO configured, route added</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vue → Worker API | Fetches jobs from external API |
| Cache → localStorage | Jobs data stored locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-03-01 | Injection | API params | mitigate | Sanitize filter inputs before fetch |
| T-03-02 | Spoofing | Cache data | accept | Data from trusted API, low risk |
</threat_model>

<verification>
[ ] Jobs page renders with cards
[ ] Filters work (location, experience, company search)
[ ] Sorting works (date)
[ ] Pagination navigates
[ ] Local caching active
[ ] SEO configured
</verification>

<success_criteria>
Jobs page functional:
- Jobs display as cards from API
- Filters: location, experience range, company search
- Sort by posted date
- Pagination works
- Local caching via localStorage
- Follows Zentor design system
</success_criteria>

<output>
After completion, create `.planning/phases/01-jobs-page/01-03-SUMMARY.md`
</output>